# auto-iterate-project 修复记录

对应《六维度审查报告.md》U-01~U-49。修复原则：每批改动后跑全量自测（基线 155 用例全绿），不破坏现有 CLI 表面与 JSON 契约。验证方式：修复后全量测试套件 + 手动端到端场景（init→backlog→begin→commit→complete→check→finish、密钥脱敏、路径守卫、指纹告警）。

## 一、安全类

| ID | 修复内容 | 原因 |
|---|---|---|
| U-01 | 新建 `secrets.py`，`mask_secret_text()` 在 findings 创建时即脱敏（前6…后4字符）；commit 拒绝信息、log.jsonl、secret-scan 输出全部只含掩码 | 扫描命中行原本把密钥原文写进 `.autopilot/log.jsonl` 和 stdout，防泄漏机制自身成为泄漏源（track_state 时会随仓库推送） |
| U-02 | commit 路径检查改用 `git -c core.quotepath=false diff --cached --name-only -z`（NUL 分隔、字面路径）；`guard.py` 匹配同时做大小写敏感与不敏感比较 | quotePath 转义让 deny_paths 对中文/特殊字符文件名 fail-open，可用命名技巧绕过黑名单 |
| U-03 | `guard.py::path_allowed` 支持目录前缀模式：尾 `/` 或无通配符裸目录名都匹配目录下全部文件；补 2 个回归测试 | references/config.md 文档示例 `["src/", "tests/"]` 在旧 fnmatch 下恒不匹配，照文档配置白名单会拒绝所有提交、循环卡死 |
| U-08 | `secrets.py` 模式表补 `github_pat_`、JWT（`eyJ…`），`sk-[A-Za-z0-9_-]{20,}` 修复 `sk-proj-`/`sk-ant-` 漏报；补全 8 模式参数化测试 | 最常见的两类 LLM API 密钥形态（OpenAI/Anthropic 新式 key、GitHub fine-grained token）可无阻碍通过"commit 前扫描" |
| U-15 | commit 成功日志在旁路时记录 `secrets_bypassed: true` | `--allow-secrets` 旁路原本无任何审计痕迹，agent 学会传该标志即形成静默通道 |
| U-16 | `init` 后把 config.json 的 SHA-256 指纹写入 state.json；`check` 发现指纹不符时告警 | check_commands 存于仓库内且被 loop 无条件执行，篡改即任意命令执行；用户通常只审代码 diff，不会复查 config 变更 |
| U-17 | 密钥模式 import 时预编译；staged diff 改 `-U0` 不读上下文行 | 逐行逐模式 `re.search` 未预编译，大 diff 时 commit 可感知变慢；`-U0` 砍掉上下文行省内存 |
| SEC-10/配置 | `config.py` 校验 `secret_patterns` 每项可编译，非法正则干净报错（另有 `secrets.compile_patterns` 运行时兜底） | 用户自定义灾难性回溯/非法正则会让 commit 直接 Traceback 或挂死 |
| U-42 | `report/retrospective --output` 限制在目标仓库内，仓库外需 `--force`；补 2 个测试 | 原实现可写任意绝对路径并静默覆盖文件，agent 自主调用时可破坏用户文件 |

## 二、正确性 / Bug 修复

| ID | 修复内容 | 原因 |
|---|---|---|
| U-11 | `_porcelain_entries` 检查 `git status` 退出码，非零时 `[ERROR]` + exit 2 | 原来失败返回空列表 → `working_tree_dirty` 误报"干净"，脏树保护护栏在异常环境（.git 损坏等）静默失效 |
| U-13 | 新增 `_unquote_git_path()`：按字节解码 git 八进制转义再按 UTF-8 重组（替代 `unicode_escape`）；配合 quotepath=false 后仅在仍被引号包裹时触发；异常收窄 | 旧解码把 UTF-8 字节按 Unicode 码点解码，中文文件名变 mojibake，污染 dirty 判定与路径匹配 |
| U-35 | `load_state` 增加 `_validate_state_types`：schema/计数器/history/current_round/type_stats 等字段类型校验，损坏值干净报错引导 init | 原来字段类型损坏（如 `"completed_rounds": "x"`）在命令中途抛 TypeError Traceback，违背自身"clear error"承诺 |
| U-34 | `_pid_alive` 的 tasklist 输出解析失败时返回 True（视为进程存活，保留锁） | 与 subprocess 失败分支语义相反：解析失败会被当"进程已死"误删活锁 |
| U-14 | 锁改为 `os.open(O_CREAT\|O_EXCL)` 原子创建，抢锁失败→读锁→判 stale→unlink→重试一次；tasklist 超时 15s→3s | 原 exists→unlink→replace 三步存在 TOCTOU：两进程可同时通过检查互相覆盖锁文件，导致并发双跑、state 互覆、重复提交 |
| U-19 | `cli.py --candidates-per-round` 帮助文本 (default 1) → (default 3) | 与 `config.py` 真实默认值漂移，误导用户与 agent |

## 三、无界增长与性能

| ID | 修复内容 | 原因 |
|---|---|---|
| U-05 | `append_log` 超过 5MB 轮转为 `log.jsonl.1`；写失败向 stderr 打 `[WARN]` | log.jsonl 永久 append 从不读取也从不清理，长跑必然膨胀；写失败静默丢审计日志 |
| U-06 | `state.append_history()`：文本字段截断至 2000 字符、history 保留最近 100 条（`io.HISTORY_LIMIT`）；5 处追加点全部接入 + 单元测试 | history 数组无界增长且每次命令全量读写，`max_rounds=None` + expand 模式下无界 |
| U-40 | phase report 只保留最近 3 份 | 每 10 轮累积一个文件从不清理 |
| U-41 | `update_candidates_status()` 批量更新（一次读一次写）；begin-round 传已加载 backlog；report/retrospective 复用一次 load | 多候选轮次逐个全量读写 backlog.json；报告函数重复 load 两次 |
| U-18 | `check`：`has_commits` 只算一次（detached 判定复用）；identity 检查移出 check（commit/diagnose 处仍强制）；`git_identity_ok` 两次 `git config` 合并为一条 `--get-regexp` | check 是循环内最高频命令，7-8 个 git 子进程纯开销 |
| U-04 | 删除死代码 `io._numstat_total`；token 估算收敛为 `io.estimate_tokens_for_round(repo, start_sha, worktree_baseline)` 唯一实现，魔数提为 `TOKEN_BASE/TOKENS_PER_TEXT_LINE/TOKENS_PER_BINARY_FILE` 常量，`commands._resolve_tokens` 改为 3 行转发 | 同一公式 `500+text*12+binary*100` 两处硬编码且 io 版是死代码，改一漏一必分叉 |

## 四、重构（行为不变）

| ID | 修复内容 | 原因 |
|---|---|---|
| U-10 | commands.py 拆出 `secrets.py`（密钥扫描）、`guard.py`（路径守卫）、`verify.py`（验证命令发现）；commands 回归纯编排 | God file（1295 行）：命令分发之外混入 4 类业务策略，无法独立测试与复用 |
| U-25 | 轮次收尾统一为 `_close_round()`：计数器、token、history、候选状态、type_stats、持久化一处完成；complete/block/cancel/finish 四处 ~30 行样板收敛 | 四段几乎同构的复制粘贴，新增轮次状态需同步改 4 处 |
| U-29 | `ensure_branch_impl`→`ensure_branch`；`compute_stop_reason/all_goals_met/rank_candidates/build_report/build_retrospective` 的 `config` 形参改名 `cfg` | 形参遮蔽同名 `import config` 模块，函数体内一旦调用 `config.xxx` 即炸 |
| U-12 | 新增 `state.default_state()` 单一权威（含 `config_fingerprint` 字段），cmd_init 与 migrate_state 共用 | state.json 默认值在两处各写一份，新增字段漏改即新旧 run schema 漂移 |
| U-33 | cli.py 改 `set_defaults(func=...)` 单点注册，删除第二个 handlers dict；`main` 只 build_parser 一次 | 命令名双注册表可能失同步；parser 构建两次（~200 个 add_argument 重复执行） |
| U-32 | io.py 内 `import io` → `import io as _stdlib_io` | 包内模块名与标准库 io 同名的防御性处理（把 scripts/autopilot 加入 sys.path 的场景下会自引用） |
| U-30/37 | 新增 `io.autopilot_file_for()`、`RETROSPECTIVE_FILENAME`、`PHASE_REPORT_INTERVAL/KEEP`、`RUN_ID_LENGTH`、`HISTORY_LIMIT/TEXT_LIMIT`、`LOG_ROTATE_BYTES`、`TASKLIST_TIMEOUT` 常量；`_is_autopilot_path`/`ensure_git_exclude` 改用 `AUTOPILOT_DIR` 常量 | `.autopilot` 字面量、retrospective.md 文件名、%10 周期、run_id 长度等魔法值散落多处 |
| U-44 | 入口脚本顶部显式 `sys.path.insert(0, scripts/)` | 原依赖 sys.path[0] 隐式机制，`python -P`/PYTHONSAFEPATH 或只复制单文件时 import 断裂 |
| U-31 | 薄入口去掉无人使用的 `EMPTY_TREE` re-export；`__init__.py` 补 `__all__` | 死导出与 wildcard-import 泄漏 |

## 五、文档与工程化

| ID | 修复内容 | 原因 |
|---|---|---|
| U-20 | `--dry-run` 补齐 7 个命令（backlog-add/update/remove/pick、directive-add、ensure-branch、detect-verify --apply、analysis-save） | SKILL.md 承诺"所有状态变更命令都支持 --dry-run"，agent 照做会报 unrecognized arguments |
| U-21 | 新增 `.github/workflows/ci.yml`（ubuntu/windows × py3.8/3.13：py_compile + 全量测试） | 无 CI：1710 行测试资产与 3.6 兼容纪律全靠人肉 |
| U-22 | SKILL.md frontmatter 加 `version: 1.0.0`；README 标注版本；新增 CHANGELOG.md | 无版本号/发布机制，跨 agent 市场分发后不可追溯 |
| U-45 | agents/openai.yaml 补 `name`/`version` | 元数据过薄且与 SKILL.md 不同步 |
| U-38 | secret-scan 非 JSON 模式的 findings 改走 stderr | 密钥线索打到 stdout 与文本消费者混流 |
| 文档 | config.md 头部声明"默认值以 config.py 为准"+ check_commands 信任链安全注记；SKILL.md check 警告清单更新（detached HEAD、config 指纹） | 消除默认值多处重复的漂移面、记录新行为 |
| U-43 | 接受现状（锁文件 hostname/pid 仅 crash 残留时泄露内网主机名，无利用面），CHANGELOG 未列为行为变更 | 评估后不改代码成本收益更优 |

## 六、明确不修（评估后保留）

- **U-23**（全量类型注解）：churn 大、零行为收益，与本仓库 3.6 兼容纪律冲突；已用字段类型校验（U-35）+ 常量集中（U-37）覆盖主要风险面。
- **U-24**（长函数逐个拆分）：`cmd_init/cmd_commit` 的核心复杂度已通过 U-10 模块拆分与 U-25 收敛显著下降，进一步拆分为纯风格改动。
- **U-36**（深嵌套压平）、**U-39**（已随 U-33 完成）、**U-46**（init 布尔开关单向：init 重跑会重置 config，文档已有路径）、**U-47**（保持 3.6 支持声明，CI 用 3.8 验证语法兼容面）。
- **U-49 部分**：token 常量断言保持现状（防回归设计），仅配置校验补了参数化矩阵测试。

## 七、验证结论

- 修复前基线：155 用例全绿（24 分钟）。
- 修复后全量套件：见测试输出（155 + 新增约 25 用例）。
- 手动端到端：完整轮次流、密钥掩码输出（`KEY = ...PLE"`）、allow_paths 目录形式（`src/`、`src`）、deny_paths 拦截非 ASCII 文件名（新密钥.pem）、config 指纹告警，全部符合预期。
- 新增测试清单：backlog-pick 4 用例、secret 模式参数化（8 模式 + 负样例 + 旁路审计 + 非法正则）、revert 冲突、失败路径 8 项（损坏锁/坏 state/坏 analysis/非 git 目录/detached HEAD/push 失败/ensure-branch 拒绝）、allow_paths 目录形式 3 项、config 指纹 2 项、--output 守卫 2 项、JSON 契约 4 项、config 校验矩阵 10 行、history 截断单元测试、--dry-run 5 项、测试环境 git 隔离。
